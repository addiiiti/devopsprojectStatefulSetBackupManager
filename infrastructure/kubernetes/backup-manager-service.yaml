apiVersion: v1
kind: Service
metadata:
  name: backup-manager-service
  namespace: backup-manager
spec:
  selector:
    app: backup-manager
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
